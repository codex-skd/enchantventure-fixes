<h2>v0.0.0-beta.3 - Data fixes for fokus and nerologistics</h2>

<h3>Fixed</h3>
<ul>
<li><strong>fokus</strong>: corrected <code>nxf/11z33h0q4</code> (obsolete <code>minecraft.play_one_minute</code> criterion renamed to <code>minecraft.play_time</code>), <code>nxf/kp4o6wh</code> (unresolved <code>schedule function $this.file</code> placeholder) and <code>nxf/_ignored_bffhan5h</code> (legacy <code>replaceitem entity @s weapon</code> → <code>item replace entity @s weapon.mainhand</code>).</li>
</ul>

<h3>Removed</h3>
<ul>
<li><strong>nerologistics</strong>: dropped the datapack override of the <code>configurator</code> recipe — the mod ships a valid one, and the override used the pre-1.21.2 ingredient format that failed to parse ("Map entry 'I'...").</li>
</ul>

<hr>

<p><strong>ZIP</strong>: <code>EnchantVenture_fixes-0.0.0-beta.3.zip</code></p>
