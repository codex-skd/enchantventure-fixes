<h2>v0.0.0-beta.1 - Initial versioned release</h2>

<h3>Added</h3>
<ul>
<li><strong>EnchantVenture Fixes</strong>: first versioned release of the datapack (server-side, pack format 107 / Minecraft 26.2).</li>
<li>tntfoundry: 6 advancements fixed (<code>hollow</code>, <code>paperwork</code>, <code>precision</code>, <code>remote_work</code>, <code>the_big_one</code>, <code>full_catalogue</code>).</li>
<li>formationsoverworld: smithing loot tables for <code>stone_tower</code> and <code>witch_tower</code> now drop <code>minecraft:iron_ingot</code>.</li>
<li>fokus: added <code>is_night</code> predicate.</li>
<li>nerospace: added <code>guide/new_life</code> breeding advancement.</li>
<li>nerologistics: restored the <code>configurator</code> recipe.</li>
</ul>

<h3>Notes</h3>
<blockquote>ageforged_armor is not included: its fixes live in the mod itself. The zip loads directly into <code>world/datapacks/</code> (pack.mcmeta at root).</blockquote>

<hr>

<p><strong>ZIP</strong>: <code>EnchantVenture_fixes-0.0.0-beta.1.zip</code></p>
